[project]
name = "tongue-diagnosis-tuning"
version = "0.1.0"
description = "TCM Tongue Diagnosis LLM Parameter Tuning Framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.0",
    "pandas>=2.1",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = []
